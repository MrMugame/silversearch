{
    "name": "silvesearch",
    "version": "1.2.3",
    "workspaces": [
        "worker",
        "modal"
    ],
    "scripts": {
        "build": "npm run build --workspace=modal && ./scripts/update_version.sh && npm run build --workspace=worker"
    }
}